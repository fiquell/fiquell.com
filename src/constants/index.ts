import { bebasneue, opensaucesans } from "./fonts";
import { developer, end, front } from "./intro";
import { linkdata, metadata, title } from "./metadata";

export {
  bebasneue,
  developer,
  end,
  front,
  linkdata,
  metadata,
  opensaucesans,
  title,
};
